# Secrets — never commit API keys (OpenAI / Gemini etc.)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output (Astro)
dist/
.astro/

# Deploy
.vercel/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Browser test artifacts (Playwright screenshots / logs) and root-level scratch images
.playwright-mcp/
/*.png

# Preview folder — visuals I open for Dianne to review; never committed
_preview/

# SpecStory auto-save (Cursor history, not source)
.specstory/

# Personal drafts (case study etc. — keep out of the public repo)
CASE_STUDY*
_to-delete/

# Local-only Chinese originals of the thinking docs (English versions live in the repo)
docs/_zh-originals/
